spring:
  config:
    import: "classpath:SECRET.yml"
  profiles:
    active: local
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
openweatherapi:
  config:
    import: "classpath:SECRET.yml"
  api:
    base-url: https://api.openweathermap.org/data/2.5
